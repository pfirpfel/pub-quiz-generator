<%_ if (question.hasOwnProperty('markdown') && question.markdown !== null) { -%>
<section data-category-number="<%= question.category_number %>" data-markdown>
    <script type="text/template">
<%_ if (question.title) { -%>
## <%= question.title %>
<%_ } -%>
<%- question.markdown %>
    </script>
</section>
<%_ } else { -%>
        <section data-category-number="<%= question.category_number %>">
            <%_ if (question.title) { -%>
                <h2><%= question.title %></h2>
            <%_ } -%>
            <%_ if (question.image) { -%>
                <img class="r-stretch" data-src="<%- question.image %>">
            <%_ } -%>
            <%_ if (question.caption) { -%>
                <p class="caption"><%= question.caption %></p>
            <%_ } -%>
        </section>
<%_ } -%>
